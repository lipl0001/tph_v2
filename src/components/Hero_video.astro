---

---

<div class="container">
  <a href="/products">
    <div class="knap">
      <button>EXPLORE PRODUCTS</button>
    </div>
  </a>
  <video class="hero_video" width="1512" height="1080" autoplay loop muted playsinline>
    <source src="https://www.projectjohansson.dk/kea/thepanichood/video/HeroVideoDesktop.mp4" type="video/mp4" />
  </video>
  <video class="hero_video_mobil" width="756" height="1344" autoplay loop muted playsinline>
    <source src="https://www.projectjohansson.dk/kea/thepanichood/video/HeroVideoMobil.mp4" type="video/mp4" />
  </video>
</div>
<style>
  .container {
    position: relative;
    width: 100%;
    height: auto;
  }

  .hero_video {
    width: 100%;
    height: auto;
  }

  .hero_video_mobil {
    display: none;
  }

  .knap {
    position: absolute;
    top: 70%;
    left: 50%;
    transform: translate(-50%, -50%); /* Centrerer elementet */
    background-color: transparent;
    z-index: 1000;
  }

  button {
    padding: 5%;
    width: 125%;
    background-color: var(--accentColor);
    border-color: var(--notThatBlack);
    border: thin;
    cursor: pointer;
    font-family: var(--primary);
    color: var(--delicateWhite);
    font-size: 1.5cqw;
    z-index: 1000;
  }

  .knap a {
    text-decoration: none;
    background-color: transparent;
    color: var(--delicateWhite);
    cursor: pointer;
    z-index: 1;
  }

  button:hover {
    text-decoration: underline;
  }

  @media screen and (max-width: 800px) {
    .hero_video {
      display: none;
    }

    .hero_video_mobil {
      display: inline;
      width: 100%;
      height: 100%;
    }

    .knap {
      top: 40%;
    }

    button {
      padding: 5%;
      width: 125%;
      background-color: var(--accentColor);
      border-color: var(--notThatBlack);
      border: thin;
      cursor: pointer;
      font-family: var(--primary);
      color: var(--delicateWhite);
      font-size: 4cqw;
    }
  }
</style>
